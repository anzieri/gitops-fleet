apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-springboot
  name: demo-springboot-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: demo-springboot
  type: ClusterIP
status:
  loadBalancer: {}
